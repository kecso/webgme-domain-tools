# Tutorials

Short, copy-paste scenarios. For flags, use `webdot <command> --help` or the [CLI reference](../CLI.md).

| Tutorial | What you learn |
|----------|----------------|
| [Run a plugin from anywhere](plugin-anywhere.md) | `--plugin-dir` + `--webgmex` without a studio catalog |
| [Install GenerateMetaTs](install-generate-meta-ts.md) | User registry install + type generation |
| [Session edit and save](session-workspace.md) | Multi-step edits with explicit save/discard |
| [History and branches](history-branches.md) | v2 `.webgmex` log, checkout, create branch |

After `npm install -g webgme-domain-tools`, these files ship in the package under `docs/tutorials/` (next to the installed module). On GitHub they live in this folder.
